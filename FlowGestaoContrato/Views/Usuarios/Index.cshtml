@*@model IEnumerable<FlowGestaoContrato.Models.Usuario>

@using FlowGestaoContrato.Models*@

@{ ViewData["Title"] = "Pagina Inicial"; }


<div class="tabelas">

    <partial name="_Confirmacoes" />

    <div class="collection with-header">
        <div class="collection-header grey darken-1">
            <div class="row">
                <div class="col s10">
                    <h6 class="white-text">Pagina Inicial</h6>
                </div>
                @*<div class="col s2">

                </div>*@
            </div>
        </div>

        @*<div class="collection-item">
            <table class="striped highlight">
                <thead>
                    <tr>
                        <th>
                            Nome
                        </th>
                        <th>
                            CPF
                        </th>
                        <th>
                            Email
                        </th>
                        <th>
                            Telefone
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.UserName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CNPJ)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Email)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PhoneNumber)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>*@

    </div>
</div>

@section Scripts {
    <script src="~/js/OperacoesUsuario.min.js"></script>
}